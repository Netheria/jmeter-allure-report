name: Java CI & Release

on:
  push:
    # runs on every push to main
    branches: [ main ]
  workflow_dispatch:
    # allows you to trigger manually in the Actions UI

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Grab your code
      - uses: actions/checkout@v3

      # 2. Install Java 8
      - name: Set up JDK 8
        uses: actions/setup-java@v3
        with:
          java-version: '1.8'
          distribution: 'temurin'

      # 3. Build your project
      - name: Build with Maven
        run: mvn clean package --batch-mode

      # 4. Upload the resulting JAR(s) so you can download them from the Actions run
      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: jmeter-allure-report
          path: target/*.jar

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')   # only when tag like v1.2.3 pushed

    steps:
      - uses: actions/checkout@v3

      # Download the artifact from the ‘build’ job
      - name: Download Build Artifact
        uses: actions/download-artifact@v4
        with:
          name: jmeter-allure-report

      # 6a. Create a GitHub Release
      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref }}          # only when tag like v1.2.3 pushed
          name: Release ${{ github.ref }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 6b. Upload the JAR to that Release
      - name: Upload JAR to Release
        uses: softprops/action-gh-release@v1
        with:
          files: target/*.jar
          tag_name: ${{ github.ref }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
